import React, { Component } from 'react';

/*
  The component renders the global header.
 */
class Header extends Component {
  render() {
    return (
      <header className="header">
        Header
      </header>
    );
  }
}

export default Header;
